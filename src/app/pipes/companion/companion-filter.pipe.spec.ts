import { CompanionFilterPipe } from './companion-filter.pipe';

describe('CompanionFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CompanionFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
